# Base URL da API Adopet (produção). Usado pelo formulário "Solicitar parceria" na landing.
# Sem barra no final. Se vazio, o build usa o default em src/api/partnershipRequest.ts
VITE_API_URL=https://adopet-api-six.vercel.app
